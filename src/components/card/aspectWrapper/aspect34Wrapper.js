export default function aspect34Wrapper({children,style,className=''}){
    return (
        <div style={style} className={`aspect-3-4-image-wrapper ${className}`}>
            {children}
        </div>
    )
} 