export default function aspect47Wrapper({children,style,className=''}){
    return (
        <div style={style} className={`retained-4-7-image-wrapper ${className}`}>
            {children}
        </div>
    )
} 